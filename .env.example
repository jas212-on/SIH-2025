# Base URL of the deployed FastAPI backend. No trailing slash.
# The whole frontend talks to the backend exclusively through this URL.
# If unset, the app falls back to http://localhost:8000 for local dev.
VITE_API_URL=
